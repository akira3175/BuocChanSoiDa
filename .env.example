# Copy to ".env" in this repo root so `docker compose` can substitute build args for frontend.
# docker compose --env-file .env up --build frontend
VITE_API_URL=http://YOUR_PUBLIC_IP:8000/api
VITE_PAYPAL_CLIENT_ID=
VITE_PUBLIC_BASE_URL=
FRONTEND_PORT=80
